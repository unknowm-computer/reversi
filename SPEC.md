# 잔나비 vs 베짱이 리버시 개발 명세

이 문서는 `PLAN.md`의 기획을 구현 및 검증 가능한 요구사항으로 정리한 문서다. **필수**는 원문 요구사항, **제안**은 원문에 없는 세부 동작 또는 모호한 부분을 보완한 구현 기준이다. 제안은 제품 요구사항으로 확정된 값이 아니다.

## 1. 목표와 개발 범위

- PC 및 모바일 브라우저에서 플레이하는 8×8 턴제 리버시 게임을 구현한다.
- 싱글 AI, 로컬 2인, 온라인 2인 대전을 지원한다.
- 잔나비와 베짱이의 상황별 리액션, 돌 뒤집기, BGM 및 SFX를 제공한다.
- 점수는 각 플레이어가 가진 돌의 개수이며 캐릭터와 돌 색상은 별도 정보로 관리한다.
- 계정, 랭킹, 채팅, 관전, 대전 기록 영구 저장은 현재 범위에 포함하지 않는다. 필요하면 별도 요구사항으로 추가한다.

### 개발 순서 제안

| 단계 | 구현 범위 | 완료 기준 |
| --- | --- | --- |
| 1. 플레이 가능한 기본판 | 규칙 엔진, 로컬 2인, 반응형 보드, 점수, 패스, 종료, 무르기 | 한 판을 처음부터 끝까지 진행하고 무르기 경계 조건을 검증 |
| 2. 싱글 플레이 | AI, AI 대전 무르기, 계산 중 입력 제어 | AI가 합법적인 수만 두고 AI 응답 전후 무르기가 정상 동작 |
| 3. 캐주얼 연출 | 캐릭터, 타이머, BGM/SFX, 리액션, 엔딩 | 상태별 연출과 오디오 수명 주기 검증 |
| 4. 온라인 | 방 생성/입장, 서버 판정, 동기화, 재접속 | 두 브라우저에서 동일한 게임 상태와 결과 확인 |

온라인은 최종 요구 범위에 포함한다. 단계 구분은 구현 순서이며 기능 삭제를 의미하지 않는다.

## 2. 게임 규칙

원문에서 생략한 표준 리버시 규칙을 다음과 같이 명시한다.

- 보드는 8행 × 8열, 좌표는 내부적으로 `row`, `col` 각각 `0..7`을 사용한다.
- 시작 배치는 백돌 `(3, 3)`, `(4, 4)`, 흑돌 `(3, 4)`, `(4, 3)`으로 한다. 흑이 선공이다.
- 빈칸에 돌을 놓았을 때 8방향 중 하나 이상에서 상대 돌이 연속으로 있고 그 끝에 자신의 돌이 있어야 합법적인 착수다.
- 유효한 모든 방향에서 포위된 상대 돌을 뒤집는다. 빈칸 또는 보드 경계에서 끊긴 방향은 뒤집지 않는다.
- 기존 돌이 있는 칸, 뒤집을 돌이 없는 칸, 자신의 턴이 아닌 상태에서는 착수할 수 없다.
- 현재 플레이어의 합법 수가 없고 상대에게 합법 수가 있으면 자동 패스하고 이유를 표시한다.
- 양쪽 모두 합법 수가 없으면 빈칸이 남아 있어도 종료한다. 보드가 가득 찬 경우도 같은 종료 조건에 포함된다.
- 종료 시 돌이 많은 쪽이 승리하며 개수가 같으면 무승부다. 남은 빈칸은 점수에 더하지 않는다.
- 기권하면 상대가 승리한다. 제안: 오조작 방지를 위해 확인 대화상자를 제공한다.

## 3. 모드와 시작 설정

| 항목 | 싱글 AI | 로컬 2인 | 온라인 2인 |
| --- | --- | --- | --- |
| 대전 상대 | 컴퓨터 | 같은 기기의 다른 사용자 | 방 코드로 접속한 사용자 |
| 캐릭터 | AI는 잔나비 고정 | 각자 선택 | 각자 선택 |
| 무르기 | 무제한 | 시작 전 설정 | 비활성화 |
| 상태 판정 주체 | 클라이언트 규칙 엔진 | 클라이언트 규칙 엔진 | 서버 규칙 엔진 |

시작 설정 제안:

- 싱글 사용자의 기본 캐릭터는 베짱이로 한다.
- 로컬/온라인은 동일 캐릭터 선택을 허용하지 않고 이름과 흑백 표시로 구분한다.
- 싱글 사용자와 로컬 1P는 흑, AI와 로컬 2P는 백을 기본값으로 한다. 온라인은 방 생성자가 흑을 기본값으로 한다.
- 캐릭터 및 게임 규칙 설정은 대국 시작 후 변경하지 않는다.
- AI는 우선 단일 난이도로 제공하고 난이도 선택 UI는 후속 범위로 둔다.

## 4. 상태와 턴 처리

제안 상태: `setup → playing → finished`. 온라인 대기실은 별도 `lobby` 상태로 관리한다.

`playing`의 입력 상태는 `awaitingMove`, `aiThinking`, `animating`, `reconnecting`으로 구분한다.

착수 처리 순서:

1. 게임 진행 여부, 턴 소유권, 입력 잠금, 좌표의 합법성을 검증한다.
2. 무르기 가능한 상태를 기록하고 착수 및 뒤집기를 한 번의 상태 변경으로 반영한다.
3. 점수와 합법 수를 재계산하고 다음 턴, 자동 패스 또는 종료를 결정한다.
4. 확정된 결과를 기준으로 연출 이벤트를 생성한다.
5. 로컬에서는 연출 종료 후 다음 입력 및 타이머를 시작한다. AI 차례면 계산을 시작한다.

- 연출 완료 여부가 규칙 판정을 바꾸지 않아야 한다.
- 더블 클릭, 연속 탭, 이전 AI 계산 응답으로 동일 턴이 중복 처리되지 않아야 한다.
- 재시작, 무르기, 화면 이탈 시 진행 중인 AI 작업·연출·예약된 사운드를 취소한다.
- 온라인의 다음 턴 시작 및 마감 시간은 서버가 지정한다. 클라이언트 연출이 늦어져도 시간을 임의로 늘리지 않는다.

## 5. 무르기

### 5.1 싱글 AI — 필수 및 경계 동작 제안

필수: 무제한으로 사용자의 직전 착수와 그에 대한 AI 응답을 되돌린다.

이를 **사용자가 마지막으로 착수하기 직전 상태로 복귀**하는 방식으로 구현할 것을 제안한다.

- AI 응답 전이면 진행 중인 계산을 취소하고 사용자 착수만 되돌린다.
- AI 응답 후이면 사용자 착수와 이후 AI 착수를 함께 되돌린다.
- 사용자 자동 패스로 AI가 여러 번 연속 착수했어도 동일한 사용자 착수 전 시점으로 돌아간다.
- 되돌릴 사용자 착수가 없으면 버튼을 비활성화한다.
- 일반 규칙으로 종료된 판은 무르기를 허용하고 결과 화면을 닫는다. 기권 종료의 무르기는 허용하지 않는다. 컴퓨터 대전 시간 초과는 종료가 아닌 꿀밤 연출로 처리한다.

### 5.2 로컬 2인

필수: 마지막으로 놓은 돌 1수와 그 결과를 되돌리고 해당 착수자의 턴으로 복귀한다.

원문에는 ‘판당’과 ‘인당’이 혼용되어 있으므로 다음 기준으로 통일할 것을 제안한다.

- 선택지: **인당 1회 / 인당 3회 / 무제한 / 비활성화**. 기본값은 인당 1회다.
- 차감 대상은 현재 차례인 사람이 아니라 되돌리는 마지막 착수의 주인이다.
- 해당 착수 이후의 자동 패스와 일반 종료 판정도 함께 되돌린다. 패스 자체는 별도 1수로 세지 않는다.
- 무르기 사용 횟수는 게임 상태 복원과 별도로 관리하여 복원 때문에 횟수가 다시 늘어나지 않게 한다.
- 취소한 캐릭터의 머리 긁적임, 상대의 째려보기 및 전용 SFX를 재생한다.

### 5.3 공통 복원 항목

보드, 현재 턴, 점수, 마지막 착수, 패스 및 종료 상태, 턴 타이머를 일관되게 복원한다. 합법 수는 복원된 보드에서 다시 계산한다. 제안: 타이머는 되돌리는 착수 직전의 남은 시간으로 복원한다.

온라인에서는 UI와 서버 모두 무르기를 허용하지 않는다.

## 6. 타이머 — 구현 기준 제안

사용자 요청에 따라 컴퓨터 대전의 기본 시간 제한과 시간 초과 처리를 아래와 같이 변경한다.

- 시작 옵션: 제한 없음 / 턴당 30초 / 턴당 60초. 컴퓨터·로컬·온라인 대전 모두 기본값은 30초다.
- 제한 시간의 50%가 지나면 고민 리액션, 남은 시간이 5초 이하이면 긴급 리액션을 재생한다.
- 컴퓨터 대전의 시간 초과는 상대가 꿀밤을 주는 2.4초 연출과 효과음으로 처리한다. 보드·점수·차례·무르기 이력을 유지하고, 연출 후 같은 플레이어에게 선택한 제한 시간을 다시 부여한다. 연출 중 입력은 막으며 반복 초과도 동일하게 처리한다.
- 로컬·온라인의 시간 초과는 즉시 패배로 처리하지 않고 상대에게 `봐준다 / 게임 종료` 선택창을 표시한다. 선택 중 착수·무르기와 대국 타이머를 멈춘다.
- `봐준다`는 2.4초 꿀밤 후 같은 보드·점수·차례에서 제한 시간을 새로 시작한다. `게임 종료`는 시간 초과한 플레이어의 패배로 확정한다. 반복 초과에도 다시 선택하며 임의로 턴을 넘기지 않는다.
- 온라인 선택 권한은 시간 초과한 플레이어의 상대에게만 있다. 서버가 선택 상태·꿀밤 종료 시각을 관리하며 중복·오래된 명령·권한 없는 선택을 거부한다. 선택/연출 상태는 재접속 시 복원한다. 두 사람이 연결된 상태에서만 선택할 수 있다.
- 꿀밤 연출은 탭 숨김 시 일시 정지하고 복귀 후 남은 시간만 재생한다. 무르기·재시작·기권·화면 이탈 시 취소한다.
- 제한 없음에서는 시간 관련 리액션 및 경고음을 끈다.
- 로컬/싱글은 탭이 숨겨지면 타이머와 AI 진행을 일시 정지한다. 복귀 후 이어서 진행한다.
- 온라인은 탭 비활성화 및 연결 끊김 중에도 서버 타이머가 계속 흐른다.
- 남은 시간은 마감 시각을 기준으로 계산하며 `setInterval` 실행 횟수를 시간으로 간주하지 않는다.

## 7. 화면 및 입력

### 시작 화면

모드 선택, 캐릭터 선택, 무르기 및 타이머 설정, 게임 시작을 제공한다. 온라인에서는 방 생성 또는 방 코드 입력으로 대기실에 진입한다.

### 게임 화면 — 필수

- 상단: 상대 프로필, 돌 색상, 점수, 타이머, 캐릭터 리액션.
- 중앙: 8×8 보드, 착수 가능 위치, 돌 뒤집기 3D 플립.
- 하단: 본인 또는 1P 프로필, 점수, 무르기, 기권, 사운드 토글.
- 현재 차례, 자동 패스, AI 계산 중, 온라인 연결 상태를 텍스트로 표시한다.
- 결과 화면: 승자/무승부, 양쪽 점수, 종료 사유, 엔딩 연출, 다시 하기와 시작 화면 이동.
- 제안: 온라인 다시 하기는 양쪽 동의 후 시작한다.

### 반응형·접근성 제안

- 모바일 세로 화면은 상단/보드/하단 순서를 유지하며 가로 넘침 없이 보드를 정사각형으로 표시한다.
- 작은 화면이나 가로 화면에서는 스크롤을 허용하되 보드와 조작 메뉴가 잘리지 않게 한다.
- 칸은 키보드로 조작 가능한 시맨틱 요소로 구현한다. 방향키로 칸 이동, Enter/Space로 착수한다.
- 조이패드는 브라우저 표준 매핑 기준으로 십자키/왼쪽 스틱 이동과 아래쪽 버튼(A / ×) 착수를 지원한다. 스틱 데드존은 0.55, 방향 반복은 최초 320ms 이후 120ms 간격이다. 확인 버튼은 눌린 순간에만 처리한다.
- 패드 선택 칸을 테두리로 표시하고 연결 상태를 안내한다. AI 차례·착수 연출·꿀밤·재접속·팝업 중에는 입력을 막는다. 연결 또는 창 복귀와 입력 잠금 해제 후에는 중립 입력을 확인한 뒤 조작을 재개한다. 설정과 팝업은 마우스/터치/키보드로 조작한다.
- 각 칸의 좌표, 돌 색상, 착수 가능 여부를 접근성 이름으로 제공한다.
- 색상 외에도 문자 또는 아이콘으로 차례와 플레이어를 구분한다.
- 턴·패스·결과는 스크린리더 알림을 제공한다. 매초 타이머 알림은 피한다.
- `prefers-reduced-motion`에서는 플립과 과도한 캐릭터 동작을 축소한다.

## 8. 리액션과 엔딩

| 이벤트 | 판정 기준 | 필수 연출 |
| --- | --- | --- |
| 고민 | 턴 제한 시간 50% 경과 | 생각하는 동작, 시계 소리 |
| 시간 임박 | 남은 시간 5초 이하 | 초조한 동작, 심장 박동 |
| 대량 획득 | 이번 수로 뒤집은 돌 3개 이상; 새로 놓은 돌 제외 | 착수자 환호, 상대 낭패 |
| 판세 역전 | 착수 전후 점수 우열이 반대로 전환 | 역전자 춤/메롱, 상대 좌절 |
| 로컬 무르기 | 유효한 무르기 완료 | 취소자 뻘쭘함, 상대 불만 |
| 패스 | 합법 수가 없는 플레이어의 자동 패스 | 어깨 으쓱, 바람 소리 |
| 대승 | 점수 차이 기준 미정 | 승자가 패자 등에 앉는 코믹 연출 |
| 일반 승리 | 대승 이외 승리 | 패자 큰절, 승자 자축 |
| 무승부 | 최종 점수 동일 | 등을 맞대고 기진맥진 |

연출 정책 제안:

- 대승은 정상 종료 시 **20점 이상 차이**로 정한다. 기권·시간 초과 승리는 점수와 관계없이 일반 승리 연출을 사용한다.
- 역전은 직전 점수 차이와 현재 점수 차이의 부호가 바뀐 경우다. 동점 진입 및 동점 해소는 역전에서 제외한다.
- 겹치는 리액션 우선순위는 `종료 > 무르기 > 패스 > 역전 > 대량 획득 > 시간 임박 > 고민 > 기본`으로 한다.
- 상위 연출이 끝나면 현재 상태에 맞는 기본 또는 타이머 리액션으로 돌아간다. 오래된 하위 연출은 뒤늦게 재생하지 않는다.
- 각 경고는 턴당 한 번 발생하며 무르기 시 이전 연출 이벤트가 중복 재생되지 않게 한다.
- 엔딩에는 건너뛰기를 제공한다.

필요 에셋: 캐릭터별 기본/고민/초조/환호/낭패/역전/좌절/무르기/불만/패스 동작, 승리·대승·무승부 상호작용, 보드 및 흑백 돌, BGM 3종과 상황별 SFX. 에셋 형식·길이·제작 방식은 아직 미정이다.

## 9. 사운드

- 로비 BGM, 기본 게임 BGM, 빈칸 10개 이하의 막판 BGM을 제공한다.
- 기본 ↔ 막판 전환은 크로스페이드한다. 무르기로 빈칸이 11개 이상이 되면 기본 BGM으로 복귀한다.
- 착수, 연속 플립, 타이머, 무르기, 턴 전환, 버튼, 엔딩 SFX를 제공한다.
- 사용자 첫 입력 이후 오디오를 활성화한다. 자동 재생이 차단되어도 게임은 정상 진행한다.
- 제안: 사운드 토글은 BGM/SFX 전체에 적용하고 사용자 설정을 로컬에 보관한다.
- 탭 숨김 시 오디오를 중단하고 복귀 시 현재 게임 상태에 맞게 복원한다. 온라인 게임 시간은 멈추지 않는다.
- 재시작·종료·컴포넌트 해제 시 이전 BGM, 반복 경고음, 오디오 노드를 정리한다.

## 10. AI — 구현 제안

- 규칙 엔진이 제공하는 합법 수만 평가한다.
- 초기 버전은 코너 확보, 상대 이동 가능 수, 위험한 코너 인접 칸, 돌 개수를 반영하는 제한 깊이 탐색을 사용한다.
- 합법 수가 없을 때는 공통 패스 규칙을 따른다.
- 계산은 `Web Worker`로 분리하여 UI 입력과 애니메이션을 막지 않는다.
- 요청마다 게임 ID와 상태 버전을 전달하고 무르기·재시작 이후 도착한 오래된 응답은 폐기한다.
- 성능 기준 제안: 합의한 기준 기기에서 수당 1초 이내에 응답하고, 시간 예산이 끝나면 확보한 합법 수 중 최선의 수를 선택한다.

## 11. 온라인 — 구현 제안

필수: WebSocket 기반 방 생성, 코드 입장, 1:1 대전, 캐릭터 선택, 무르기 비활성화.

### 방 수명 주기

`waiting → ready → playing → finished → closed`

- 추측하기 어려운 6자리 영문/숫자 방 코드를 서버가 발급하고 중복을 검사한다.
- 최대 2명만 입장한다. 잘못된 코드, 가득 찬 방, 만료된 방은 구분해 안내한다.
- 두 사용자의 준비 완료 후 서버가 초기 상태를 발행한다.
- 재접속용 세션 토큰으로 플레이어를 식별한다. 방 코드만으로 기존 플레이어 권한을 얻을 수 없게 한다.
- 연결 해제 후 재접속 유예는 30초다. 타이머 만료 시 상대의 선택 대기로 전환하며, 선택 대기와 꿀밤 중에도 재접속 유예 만료는 연결 종료 패배로 처리한다.
- 양쪽 모두 유예 시간 내 복귀하지 않으면 방을 종료한다. 대기 중 방은 10분 동안 활동이 없으면 정리한다.

### 서버 권한 및 메시지

- 서버가 착수, 턴, 패스, 점수, 타이머, 승패의 최종 판정 주체다.
- 클라이언트는 완성된 보드를 보내지 않고 착수 좌표 등 의도만 전송한다.
- 공통 규칙 엔진을 클라이언트와 서버가 공유하되 서버에서 모든 요청을 다시 검증한다.
- 변경 요청은 `requestId`, 게임별 `gameId`, `expectedRevision`을 포함한다. 중복 요청과 오래된 상태의 요청을 거부하거나 기존 결과로 응답한다.
- 상태에는 증가하는 `revision`을 포함한다. 작은 보드이므로 초기 버전은 변경마다 전체 상태 스냅샷을 전송한다.
- 재접속 시 서버 스냅샷으로 복구하고 오래된 이벤트는 적용하지 않는다.
- 클라이언트 요청 예: `room:create`, `room:join`, `player:ready`, `game:move`, `game:resign`, `game:rematch`, `session:resume`.
- 서버 응답 예: `room:state`, `game:state`, `request:error`. 오류에는 요청 ID와 기계적으로 구분 가능한 오류 코드를 포함한다.
- 메시지 스키마 검증, 방 코드 탐색 및 착수 요청의 빈도 제한을 적용한다.

## 12. 기술 구조 제안

기획의 Vue 3 + TypeScript + Pinia + SCSS를 적용한다. 빌드 도구는 Vite, 오디오는 Web Audio API, 온라인은 Node.js + Socket.IO를 초기 선택으로 제안한다. 캐릭터는 에셋 제작 방식에 따라 CSS 또는 Lottie를 선택한다.

| 위치 | 책임 |
| --- | --- |
| `shared/game/types.ts` | 돌, 좌표, 플레이어, 설정, 결과의 타입 |
| `shared/game/rules.ts` | 합법 수, 뒤집기, 패스, 종료 판정의 순수 함수 |
| `shared/protocol.ts` | 온라인 요청/응답 타입 및 검증 계약 |
| `src/stores/game.ts` | 로컬 게임 상태 및 무르기 이력 |
| `src/composables/useGameController.ts` | 모드별 착수, 패스, 종료 흐름 조정 |
| `src/composables/useTurnTimer.ts` | 턴 시간 및 경고 상태 |
| `src/composables/useGameAudio.ts` | BGM/SFX 재생과 정리 |
| `src/composables/useCharacterReaction.ts` | 리액션 우선순위 및 이벤트 소비 |
| `src/composables/useOnlineRoom.ts` | 소켓 연결, 방, 재접속 및 상태 동기화 |
| `src/workers/reversiAi.worker.ts` | AI 탐색 |
| `src/components/game/` | 보드, 칸, 플레이어 패널, 조작 메뉴, 결과 화면 |
| `src/components/common/` | 여러 화면에서 재사용하는 버튼·대화상자 등 |
| `server/` | 방 관리, 세션, 서버 타이머, 게임 명령 처리 |

데이터 계약:

- `Cell`: `null | 'black' | 'white'`. 보드는 항상 64칸을 가진다.
- `GameSettings`: 모드, 플레이어별 캐릭터/색상, 턴 제한, 무르기 제한.
- `GameState`: 게임 ID, 상태 버전, 보드, 현재 턴, 진행 상태, 마지막 착수, 결과.
- `GameResult`: 승자 또는 무승부, 양쪽 돌 개수, 종료 사유(`noLegalMoves`, `resign`, `timeout`, `disconnect`).
- `HistoryEntry`: 착수자와 착수 직전 규칙 상태 및 타이머. 무르기 소비 횟수는 별도 보관한다.
- `GameEvent`: 착수, 뒤집기, 패스, 역전, 무르기, 종료의 식별 가능한 이벤트. UI와 오디오는 이를 소비한다.
- 점수·합법 수는 보드에서 계산하여 중복 상태 간 불일치를 피한다.

새 코드는 TypeScript, Vue는 `<script setup lang="ts">`, 스타일은 `<style lang="scss">`를 사용한다. props는 interface, emits는 call-signature로 작성한다. 전역 디자인 토큰과 반응형 breakpoint를 중앙에서 관리한다.

## 13. 검증 및 완료 기준

### 규칙 엔진

- 초기 배치에서 흑의 합법 수가 4개다.
- 8방향 각각과 여러 방향을 동시에 포위하는 착수의 뒤집기 결과가 정확하다.
- 빈칸·경계·자기 돌로 시작하는 방향은 잘못 뒤집지 않는다.
- 불법 착수가 보드와 턴을 변경하지 않는다.
- 한쪽만 착수 불가이면 패스하고, 양쪽 모두 착수 불가이면 남은 빈칸과 관계없이 종료한다.
- 정상 종료, 동점, 기권 및 시간 초과 결과를 구분한다.

### 모드 및 상태

- 싱글 무르기를 AI 응답 전/후, 자동 패스 후, 마지막 수 후에 검증한다.
- 로컬 무르기에서 1수만 복원하며 실제 착수자의 횟수가 차감된다. 한도 소진 및 비활성화 설정이 적용된다.
- 무르기로 타이머·결과·턴·막판 BGM 상태가 일관되게 복원된다.
- AI 계산 중 재시작하거나 무르기를 해도 이전 응답이 새 상태에 적용되지 않는다.
- 온라인 양쪽 보드·점수·턴·결과가 일치한다. 중복 요청, 순서가 뒤바뀐 상태, 상대 턴의 요청, 잘못된 좌표를 처리한다.
- 온라인 재접속 성공/유예 만료/시간 초과와 양쪽 이탈을 검증한다.

### 브라우저 및 연출

- 최신 Chrome에서 싱글 및 로컬 대전을 한 판 끝까지 실제 조작한다.
- 모바일 크기에서 보드·메뉴가 잘리지 않는지, 키보드만으로 착수할 수 있는지 확인한다.
- iOS Safari의 실제 터치 및 오디오 활성화는 실제 기기 또는 원격 디버깅으로 별도 검증한다. viewport 에뮬레이션만으로 완료 처리하지 않는다.
- 사운드 토글, 탭 숨김/복귀, 재시작, 화면 이탈 시 소리 중복 및 잔류가 없다.
- 타입 검사, 프로덕션 빌드, 규칙/무르기 테스트, 온라인 통합 테스트가 통과한다.

## 14. 구현 전 확정할 항목

아래 항목은 원문에 없거나 해석이 갈리는 결정 사항이다. 별도 결정 전에는 본문의 값을 구현 제안으로 취급한다.

| 항목 | 제안 |
| --- | --- |
| 첫 개발 단계 | 로컬 기본판부터 순차 구현 |
| 타이머 | 모두 기본 30초. 컴퓨터는 초과 시 꿀밤 후 재개. 로컬/온라인은 상대가 봐주기 또는 종료 선택 |
| 로컬 무르기 횟수 | 판 전체가 아닌 인당 기준, 기본 인당 1회 |
| 종료 후 무르기 | 정상 종료는 허용, 기권·시간 초과는 불허 |
| 캐릭터 중복 | 로컬/온라인에서 허용 |
| 선공 배정 | 사용자/1P/방 생성자가 흑 |
| 대승 기준 | 정상 종료 시 20점 이상 차이 |
| AI 수준 | 초기 단일 난이도, 제한 시간 내 탐색 |
| 온라인 재접속 | 30초 유예, 서버 타이머 계속 진행 |
| 에셋 | 그림체, 제작 방식, 파일 형식, 동작 길이 별도 결정 |
| 배포 | 프론트 및 상시 소켓 서버 호스팅 위치 별도 결정 |


### 특수 착수 연출 (구현)

- 모서리에서 4개 이하를 뒤집으면 타격 연출 없이 돌을 뒤집고, 획득한 플레이어 카드에서 2.6초간 전용 표정을 표시한다. 잔나비는 사악한 웃음 표정과 합성 웃음소리, 베짱이는 웃는 표정과 합성 휘파람을 사용한다. 다음 착수에도 남은 표정 시간을 유지하되 무르기·새 게임에서는 해제한다.
- 모서리 여부와 관계없이 한 수에 5개 이상 뒤집을 때 공격 캐릭터가 상대에게 코믹한 타격을 가한다. 모서리와 대량 뒤집기가 겹치면 타격 연출을 우선하고 웃음·휘파람 반응은 재생하지 않는다.
- 새 돌을 먼저 표시하고 700ms 타격 연출 후 뒤집기를 시작한다. 연출 및 뒤집기 동안 착수를 잠그고 마지막 수라면 결과 화면을 뒤집기 종료까지 늦춘다.
- 싱글/로컬은 다음 턴 타이머와 AI 계산을 연출 종료 후 시작한다. 온라인은 서버 판정과 시간을 유지하고 화면에만 지연 연출을 적용한다.
- 무르기·새 게임·종료·화면 이탈 시 예약된 연출을 정리한다. 재접속으로 여러 수를 건너뛴 상태는 연출하지 않는다.
